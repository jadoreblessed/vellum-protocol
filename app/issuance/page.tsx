import Link from "next/link";
export default function Issuance(){return <main><header className="nav"><div className="container nav-inner"><Link href="/" className="wordmark">vellum</Link><Link href="/app" className="button acid">Create note</Link></div></header><section className="section container"><div className="eyebrow mono">06 / ISSUANCE</div><h1 className="section-title">One deposit.<br/><em>One document.</em></h1><p className="section-intro">Vellum records the balance delta that arrives in the vault, then mints one note with a stable serial and a readable history.</p><div className="protocol-list" style={{marginTop:70}}>{[["INPUT","ERC-20 position"],["RECORD","Actual received balance"],["PRICE","Pool TWAP at entry"],["OUTPUT","One ERC-721 note"]].map(([a,b])=><div className="protocol-row" key={a}><b>{a}</b><span>{b}</span></div>)}</div></section></main>}
