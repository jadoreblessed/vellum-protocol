import Link from "next/link";
import TokenLogo from "../components/TokenLogo";
export default function Notes(){return <main><header className="nav"><div className="container nav-inner"><Link href="/" className="wordmark">vellum</Link><Link href="/app" className="button acid">Open notebook</Link></div></header><section className="section container"><div className="eyebrow mono">02 / NOTEBOOK</div><h1 className="section-title">A claim you can<br/><em>carry in one view.</em></h1><p className="section-intro">Notes make a position portable without hiding the numbers. Quantity, entry, mark, PnL and maturity remain part of the object.</p><div className="notes-grid" style={{marginTop:70}}>{["LIVE / 001","LOCKED / 002","MATURED / 003"].map((label,i)=>{const symbol=`$${["ORBIT","MOTE","NOVA"][i]}`; const color=["var(--blue)","var(--coral)","#7a6a35"][i]; return <article className="mini-note" key={label}><div className="note-top"><strong>VLM</strong><span>{label}</span></div><div className="note-band" style={{background:color}}><div className="token-icon"><TokenLogo symbol={symbol} color={color}/></div><div><div className="token-symbol">{symbol}</div><div className="token-meta">ROBINHOOD CHAIN</div></div></div><div className="note-body"><div className="label">Position</div><div className="position">{["250,000","1,200,000","90,000"][i]}</div><div className="lock-band">{i===0?"OPEN TO TRANSFER":"CLAIMABLE · VERIFIED"}</div></div></article>})}</div></section></main>}
