import Link from "next/link";
export default function Ownership(){return <main><header className="nav"><div className="container nav-inner"><Link href="/" className="wordmark">vellum</Link><Link href="/app/note" className="button">View sample note</Link></div></header><section className="section container"><div className="eyebrow mono">07 / OWNERSHIP</div><h1 className="section-title">The chart moves.<br/><em>The claim changes hands.</em></h1><p className="section-intro">Transfer a note between wallets while the underlying position remains untouched in the vault. The new holder becomes the claimant.</p><div className="steps" style={{marginTop:70}}>{[["A","Current holder","Wallet A owns the note."],["B","Transfer","The ERC-721 changes hands."],["C","New holder","Wallet B owns the claim."],["D","Underlying","The vault balance stays put."]].map(([n,t,p])=><div className="step" key={n}><div className="step-num">{n}</div><h3>{t}</h3><p>{p}</p></div>)}</div></section></main>}
